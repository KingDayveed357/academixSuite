import { formatCurrency } from '@/lib/formatters'
export function KPICard({ label, value, subLabel, delta }: { label: string; value: number; subLabel?: string; delta?: number }) { return <article className="rounded-md border border-border bg-surface p-6 shadow-card"><p className="text-xs font-medium uppercase tracking-wide text-text-secondary">{label}</p><p className="mt-2 font-mono text-[32px] font-bold tabular-nums text-text-primary">{formatCurrency(value)}</p><p className="mt-2 text-xs text-text-secondary">{typeof delta === 'number' ? `${delta >= 0 ? '?' : '?'} ${Math.abs(delta)}% ` : ''}{subLabel ?? ''}</p></article> }
